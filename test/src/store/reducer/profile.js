import * as Types from '../action-types';

const profileReducer = (state = {}, action) => {
    state = {...state};
    switch (action.type){

    }
    return state;
};

export default profileReducer;